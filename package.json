{
  "name": "mrks-job-protocol-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.mjs && npm run generate:python",
    "generate:python": "python3 -m datamodel_code_generator --input openapi/job-protocol-v1.yaml --input-file-type openapi --output python/src/mrks_job_protocol/models.py --output-model-type pydantic_v2.BaseModel --use-standard-collections --use-union-operator --target-python-version 3.11",
    "build": "npm --prefix typescript run build",
    "test": "npm run build && node --test typescript/tests/*.test.mjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "openapi-typescript": "^7.10.1",
    "typescript": "^5.9.3"
  }
}
